// Mirrors backend defaults in auth-system/app/core/config.py. Not exposed over the API yet,
// so these are duplicated here purely for progress-bar display.
export const DAILY_CAP_MINUTES = 180;
export const WEEKLY_CAP_MINUTES = 900;

export const SUBJECTS = ["Biology", "Core Maths", "Chemistry", "English"];
