VITE_API_BASE_URL=http://localhost:8000

# Google Sign-In - the OAuth Client ID from https://console.cloud.google.com/apis/credentials
# (not a secret - safe to expose client-side). Leave unset to hide the Google sign-in button.
VITE_GOOGLE_CLIENT_ID=
